# Refer for explanation to following link:
# https://lefthook.dev/configuration/

pre-commit:
  parallel: true
  jobs:
    - name: standardrb
      glob: "*.{rb,rake,Gemfile,Rakefile,config.ru}"
      run: bundle exec standardrb --fix {staged_files}
      stage_fixed: true
